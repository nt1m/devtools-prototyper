let Menu = require('./menu');

let LibrariesMenu = React.createClass({displayName: "LibrariesMenu",
  render() {
    return (
      React.createElement(Menu, null

      )
    );
  }
});

module.exports = LibrariesMenu;
